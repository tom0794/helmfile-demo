repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
  - name: nginx
    chart: bitnami/nginx
    version: 12.0.4
    values:
    - {{ .StateValues | toYaml | nindent 6 | trim }}